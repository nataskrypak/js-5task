alert('Hello Javascript!');

let userName = prompt('enter your name');
let greetings = 'Hello! ';
alert(userName);
let userAge = parceFloat(prompt('how old are you?'));
alert(userAge);
let nextAge = add(userAge + 1);
alert('наступого вам буде: ' +nextAge);
if(userAge) >= 18{ // > < <= >= != !== || &&
alert('ви вже повнолытный');
} else {
alert('ви ще не повнолітній');
}
function add = (a, b); {
    let result a + b;
    return result;

}